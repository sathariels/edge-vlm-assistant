# No secrets — this project runs entirely on-device with no cloud APIs.
# All inference is local. This file exists only as a placeholder.

# Optional: override model directory (default: ~/models/)
# MODELS_DIR=/path/to/models

# Optional: override venv path (default: ~/glasses-project/venv)
# VENV_PATH=/path/to/venv
